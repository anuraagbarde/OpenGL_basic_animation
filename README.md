# Basic Animation using OpenGL

## Steps to run

1. Install Dev-c++ (windows)
2. New->Project->OpenGL
3. Build & Run (keep main.cpp as default)
4. Replace main.cpp with the repo code
5. Build and run.

:)
